name: Sync Content to Site

on:
  push:
    branches: [main]
    paths:
      - 'content/**'
  workflow_dispatch:

concurrency:
  group: sync-to-site
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout votes repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Checkout site repo
        uses: actions/checkout@v4
        with:
          repository: VotedForUs/site
          token: ${{ secrets.PERSONAL_TOKEN }}
          path: ./site

      - name: Sync content to site/src/data
        run: rsync -av --delete content/ site/src/data/

      - name: Check for site data changes
        id: check-changes
        run: |
          cd site
          if git diff --quiet -- src/data/; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Set PR body
        if: steps.check-changes.outputs.changed == 'true'
        run: |
          if [ -f .github/pr-body.md ]; then
            echo "pr_body_path=.github/pr-body.md" >> $GITHUB_ENV
          else
            echo "## Data Sync" > /tmp/pr-body.md
            echo "Synced congressional data from votes@${{ github.sha }}" >> /tmp/pr-body.md
            echo "pr_body_path=/tmp/pr-body.md" >> $GITHUB_ENV
          fi

      - name: Create pull request in site repo
        if: steps.check-changes.outputs.changed == 'true'
        uses: peter-evans/create-pull-request@v7
        with:
          token: ${{ secrets.PERSONAL_TOKEN }}
          path: ./site
          commit-message: "data: sync from votes@${{ github.sha }}"
          branch: sync/votes-content-${{ github.sha }}
          title: "Sync congressional data: ${{ github.sha }}"
          body-path: ${{ env.pr_body_path }}
          add-paths: src/data/**
          labels: data-sync
